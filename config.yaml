# Dashboard Generator Configuration
scan_interval: 60 # seconds
server_port: 8080 # Port to serve the dashboard on

nodes:
  - name: "Serwer Główny"
    host: "100.69.201.80"
    is_local: true
  - name: "Zdalny VPS"
    host: "100.99.158.2"
    ssh_user: "root"

exclude_ports:
  - 22    # SSH
  - 8080  # Dashboard itself
  - 53    # DNS
  - 3306  # MySQL
